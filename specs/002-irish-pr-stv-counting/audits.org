#+TITLE: W002 - Irish PR-STV counting audit evidence
#+STARTUP: content
#+OPTIONS: toc:2 num:nil

* Definition gate

- Execution :: Approved minor refresh; the prior effective definition PASS is retained by explicit operator acceptance on 2026-09-08. The unsuccessful fresh audit attempt remains history, not a new independent verdict.
- Artefact :: [[file:spec.org][W002 - Irish PR-STV counting specification]].
- Required reviews :: Local =audit-spec=, =audit-design=, and =audit-test-definitions=; then one retained external context after the local reviews pass.
- Evidence policy :: Record exact reviewed revisions, findings, and remediation here. No audit PASS is implied by the existence of the draft.

** Local review, round 1
:PROPERTIES:
:ROUND: 1
:END:

- Gate :: definition.
- Revision :: SHA-256 =5a7664dc14634f2db91cb6645dfb38e9a2e228403ccd396a2da672eea76c02d7=.
- Context :: Authoring context.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: FAIL.
- [audit-spec] [BLOCKING] Counting contract :: The draft delegates algorithm choices to future definition work instead of specifying a deliverable rule set.
- [audit-design] [BLOCKING] Reproducibility :: The rule version, canonical fingerprint, surplus order, selection, rounding and tie resolution are not explicit enough for independent implementations to agree.
- [audit-test-definitions] [BLOCKING] Missing oracles :: Historical ties, skip transfers, bulk exclusion and boundary surplus cases lack fixed expected paths.
- Remediation :: Define the online-poll algorithm and fixed examples directly, using the existing Wikipedia/Irish guidance. Legislative conformity remains outside scope.

** Local review, round 2
:PROPERTIES:
:ROUND: 2
:END:

- Gate :: definition.
- Revision :: SHA-256 =37bfc2e787233dce7796c69275866593ff8ee0e447168e54d161354a4a09d632=.
- Context :: Authoring context.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: PASS.
- Findings :: No local blocking or conditional finding remains. The online-poll procedure specifies quota, surplus priority, parcel selection, whole-ballot apportionment, rounding, initial order, lots, exclusions, exhaustion and termination.
- Remediation receipt :: Added explicit version/fingerprint conventions and fixed oracles for original/last parcels, T=0/T<U/T=U, historical-high/low comparisons, surplus and remainder lots, skip transfers, bulk-exclusion inequality and zero-tally last places. Independently checked grouped ballot totals, quotas, selected ballot IDs and resulting winners by hand.
- Limitation :: Independent audit and operator sign-off remain pending; no counting implementation or regression execution is claimed.

** External review, round 1
:PROPERTIES:
:CONTEXT: /root/counting_definition_audit
:ROUND: 1
:END:

- Gate :: definition.
- Revision :: SHA-256 =37bfc2e787233dce7796c69275866593ff8ee0e447168e54d161354a4a09d632=.
- Audits :: Requested composite =audit-spec=, =audit-design=, =audit-test-definitions= in one retained context.
- Verdict :: FAIL.
- [audit-spec] [BLOCKING] Historical fixture arithmetic :: RT002.7 incorrectly counted the C>B ballot as transferring onward to A. Only the two original B>A ballots transfer: A finishes with 5 and one ballot exhausts, not A6.
- Remediation :: Corrected the expected total and explicitly required the exhausted-ballot/conservation record.

** Local review, round 3
:PROPERTIES:
:ROUND: 3
:END:

- Gate :: definition.
- Revision :: SHA-256 =ab0ffef9c4eea6cd883760de5f675a3b49ed8c88f6a12e91383a6a37333eda97=.
- Context :: Authoring context.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: PASS.
- Findings :: No additional local blocking finding after rechecking the revised historical example and its conservation. The other skip/exhaustion fixtures already specify their exhausted ballots separately.
- Limitation :: Retained independent review of the correction remains pending; no execution evidence is claimed.

** External review, round 2
:PROPERTIES:
:CONTEXT: /root/counting_definition_audit
:ROUND: 2
:END:

- Gate :: definition.
- Revision :: SHA-256 =ab0ffef9c4eea6cd883760de5f675a3b49ed8c88f6a12e91383a6a37333eda97=, before lifecycle-only DEFINITION_GATE update.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=; the retained auditor explicitly confirmed all three were applied.
- Verdict :: PASS.
- Findings :: No further blocking or advisory finding. The corrected historical fixture accounts for A5 plus one exhausted C-origin ballot.
- Scope :: Online-poll counting rules, determinism, arithmetic and test definitions. Legislative conformity is excluded by operator direction.
- Limitation :: Operator sign-off and implementation/one-off validation remain pending.

** Operator sign-off, 8 September 2026

- Authority :: Taḋg, explicit instruction: "approved all 3. i will build in another session".
- Scope :: Approval of all three specifications, including their labelled proposals.
- Revision :: Audited specification at =ec0bfd8= (three audited build specifications for review); only lifecycle/sign-off metadata changes accompany this receipt.
- Delivery :: Reserved for another session by operator instruction. No code or implementation-test evidence is added.

** Architecture refresh, local review round 1, 8 September 2026

- Gate :: definition.
- Artefact :: W002 - Reproducible PR-STV counting.
- Revision :: SHA-256 =c1c0d2783dba21c562641b6187ae3ab3eae11d272a0417ec090806be31a39756=.
- Context :: Authoring context; all three combined criteria applied to this specification and its shared boundaries.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: PASS (local only).
- Authority :: Operator-approved current foundation and native web-linter selection, followed by the instruction to refresh the three existing specifications. No new work item or application implementation is introduced.
- Scope and receipt :: Architecture authority and approved-status wording reconciled. Counting procedure, arithmetic fixtures, fingerprints and persisted schema/rule versions are byte-for-byte unchanged; the engine introduces no web-tool dependency.
- Review :: Checked summary/detail agreement, resolved versus historical decisions, requirement/test traceability, Go component boundaries, Exodan-owned configuration and operations, security/privacy and recovery, and proportionate test definitions. No required local correction remains.
- Evidence :: Fourteen documentation files sanitize and parse through Pandoc; 214 local links/anchors resolve with unique heading identifiers. The counting procedure/fixtures/fingerprints and VISION copy are unchanged. Initial CLI/store tests and Makefile were read as implementation evidence, not executed or treated as authority. Concurrent implementation edits are preserved.
- Approval :: Existing product/interface approvals are retained. The operator explicitly approved the web-tool selection before this refresh; that approval does not substitute for the refreshed audit. Any new material decision raised by review requires operator authority.
- External continuation :: One retained external context will review this three-spec refresh, applying each combined gate separately and returning a verdict for each exact specification revision. No prior implementation audit is resumed or replaced by this definition review.

** Architecture refresh, independent runner incident, 8 September 2026

- Gate :: definition; refreshed batch of three existing specifications.
- Context :: One configured external audit invocation through =sdlc-harness start --phase audit=, using installed SDLC v3.1.11 and its configured audit harness/model/timeout. The preceding local-review hash identifies this specification's submitted revision.
- Result :: RUNNER INCIDENT; neither PASS nor FAIL. No fresh independent specification, design or test-definition verdict was accepted.
- Evidence :: The runner exited 1 with =sdlc-harness: audit harness response-malformed: composite verdict requires exactly one non-empty GATE field=.
- Cause of request mismatch :: The submitted instruction requested a separate GATE/verdict block for each of the three specifications. The runner requires one composite GATE field. This formatting request was incorrect; the rejection is not evidence of a specification defect.
- SESSION_ID :: Not returned. The command output supplied no resumable identity. The task's OS temporary directory contains only the submitted refresh diff; no project-local harness context was found through the inspected runtime paths. No replacement reviewer was started.
- Continuation :: Operator direction is required to authorize a replacement review context because the required retained context cannot be addressed. A retry must request exactly one composite verdict and identify per-spec findings within it, or use separately authorized per-spec gates. No prior implementation-audit incident is changed by this definition record.
- Approval boundary :: The approved product and architecture decisions remain recorded. This incident does not grant a current independent gate to materially changed tooling/test definitions; it does not invalidate the unchanged counting rules or their original review evidence.

** Operator approval of refreshed specification, 8 September 2026

- Authority :: Taḋg: "ok approved", after the refreshed specifications were opened for review.
- Scope :: Approval of this refreshed specification, retaining the established product/architecture decisions and native web-tool selection.
- Revision :: Specification at =3f26bde= (refresh verification and audit runner incident checkpoint), followed only by this approval metadata update.
- Gate boundary :: Human approval is recorded separately from the independent audit. The runner incident remains unresolved; no independent PASS or implementation closure is inferred. No replacement audit context has yet been started.

** Operator disposition of minor refresh, 8 September 2026

- Authority :: Taḋg described the changes as a minor lint-tool substitution and stated: "previously these passed, i am happy to mark them approved".
- Accepted revision :: SHA-256 =774d0491c7fd91d16dcebe931a84beb71618c8f4e7a4e04defecb0d73c2ccacc=, before this disposition's metadata updates.
- Disposition :: Retain the prior effective definition PASS and mark the refreshed specification approved on the operator's explicit acceptance of the limited change. No replacement independent audit is required for this refresh.
- Evidence boundary :: The previous independent passing evidence and current local review remain recorded. The failed runner invocation supplied no accepted independent verdict and is not converted into audit PASS. This operator disposition supersedes the earlier pending-gate/replacement-context requests.
- Limits :: Acceptance covers the documented architecture reconciliation and native web-tooling refresh only. It introduces no new voting, privacy or infrastructure policy and does not waive implementation tests, implementation audit, required validation or operator closure.

* Implementation gate

- Execution :: Active-off delivery resumed on 2026-09-08. Audit counts restart for this invoking session; earlier implementation evidence remains historical.

** Local implementation review, resumed-session round 1
:PROPERTIES:
:ROUND: 1
:END:

- Gate :: implementation.
- Revision :: =8f909bd=.
- Context :: Delivery context.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: FAIL.
- [audit-test-code] [BLOCKING] RT002.3, RT002.4 and RT002.10 - Detailed count-record evidence :: Fixed fixtures do not yet assert quotient/remainder calculations, selected ballot identifiers, per-operation non-transferable change, or selection provenance.
- [audit-code] [BLOCKING] AC002.12 - Cancellation granularity :: The engine checks cancellation between count operations but not within ballot validation and transfer scans as required by the signed-off progress boundary.
- [audit-code] [BLOCKING] AC002.10 and AC002.12 - Runtime invariants :: The engine records cumulative totals but does not fail closed on an internal ballot-conservation violation.
- [audit-code] [BLOCKING] Counting transition maintainability :: =Run= coordinates validation, election, surplus choice, exclusion choice, transfer and result projection in one oversized function, obscuring the invariants that the unusual counting rules require.
- Remediation :: Add fixed detailed-record assertions; carry cancellation through ballot scans; enforce conservation before emitting each record; and extract cohesive transition helpers without changing the signed-off rule behaviour. Re-run all affected tests and the full composite local review only after every finding is addressed.

** Local implementation review, resumed-session round 2
:PROPERTIES:
:ROUND: 2
:END:

- Gate :: implementation.
- Revision :: =8f909bd= plus the remediated working tree.
- Context :: Delivery context.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: FAIL.
- Remediation receipt :: The engine now records exact surplus apportionments and ordered selection provenance, checks cancellation throughout ballot scans, enforces runtime conservation, and decomposes =Run= into a 49-line coordinator with cohesive transition methods. =make test=, =make lint=, =make vulncheck= and focused count coverage at 91.9% pass on the remediated candidate.
- [audit-test-code] [BLOCKING] RT002.1 and RT002.2 - Input and quota fixtures :: Duplicate ballot IDs, empty ballots, winner-count limits, unsupported rules, and the two-place/three-place quota expectations remain unasserted.
- [audit-test-code] [BLOCKING] RT002.3 through RT002.5 - Transfer branch fixtures :: Largest-surplus priority, the alternate last-parcel lot outcome, and transfers skipping excluded or elected options need exact retained assertions.
- [audit-test-code] [BLOCKING] RT002.7 - Invalid decisions :: Wrong sequence, kind, fingerprint, selection cardinality and ineligible choices are not each shown to fail closed.
- [audit-test-code] [BLOCKING] RT002.8 - Generated replay :: Fixed fixtures establish conservation and JSON replay, but the specified bounded seeded generated supplement is absent.
- [audit-test-code] [BLOCKING] RT002.9 - Declared bounds :: The 50-option/1000-ballot maximum and ballot overflow are covered; option overflow remains unasserted.
- Remediation :: Add the complete missing fixture matrix with independent expected outcomes and deterministic generation, then rerun the full suite, lint, vulnerability check and composite review.

** Local implementation review, resumed-session round 3
:PROPERTIES:
:ROUND: 3
:END:

- Gate :: implementation.
- Revision :: =baef447=.
- Context :: Delivery context.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: PASS.
- Remediation receipt :: The retained suite now covers all round-2 gaps, including every input and decision boundary, one/two/three-place quota cases, largest and equal surpluses, original and last parcels, remainder-boundary lots, skip transfers, strict bulk exclusion, last vacancies, cancellation, declared maxima, exact fingerprints, detailed records, JSON replay and 100 deterministic generated cases using seed =20260908=.
- Test evidence :: =make test= passed with the race detector; =make lint= passed Go, CSS and rendered-HTML checks; =make vulncheck= reported no vulnerabilities; focused count coverage was 91.9% before the final expanded matrix and remained above the 80% floor. All results were observed against the candidate committed as =baef447=.
- Code review :: =Run= is a 49-line coordinator over cohesive election, surplus, exclusion, decision and record stages. Cancellation propagates through ballot scans, conservation fails closed, no hidden randomness or external dependency exists, and result/request ordering and fingerprints follow the signed-off versioned contract.
- Findings :: No blocking or conditional local finding remains. External composite implementation audit and final validation remain pending.

** External implementation review, runner incident, 8 September 2026
:PROPERTIES:
:ROUND: 1
:END:

- Gate :: implementation.
- Revision :: =baef447=.
- Context :: One external context invoked through the =audit-implementation= workflow and =sdlc-harness start --phase audit=.
- Audits :: Composite =audit-test-code= and =audit-code= only. The signed-off specification was supplied as authority and was explicitly excluded from renewed definition review.
- Result :: RUNNER INCIDENT; neither PASS nor FAIL.
- Evidence :: The runner exited 1 with =sdlc-harness: audit harness response-malformed: unsupported VERDICT "UNAVAILABLE"=.
- SESSION_ID :: Not returned. No retained external context identity is available to resume.
- Count :: This is not a failed audit round because the harness accepted no composite implementation verdict.
- Continuation :: Do not create a replacement external context. Operator direction is required because the mandated retained context cannot be addressed.
