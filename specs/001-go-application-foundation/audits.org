#+TITLE: W001 - Go application foundation audit evidence
#+STARTUP: content
#+OPTIONS: toc:2 num:nil

* Definition gate

- Execution :: Approved minor refresh; the prior effective definition PASS is retained by explicit operator acceptance on 2026-09-08. The unsuccessful fresh audit attempt remains history, not a new independent verdict.
- Artefact :: [[file:spec.org][W001 - Go application foundation specification]].
- Required reviews :: Local =audit-spec=, =audit-design=, and =audit-test-definitions=; then one retained external context after the local reviews pass.
- Evidence policy :: Record exact reviewed revisions, findings, and remediation here. No audit PASS is implied by the existence of the draft.

** Local review, round 1
:PROPERTIES:
:ROUND: 1
:END:

- Gate :: definition.
- Revision :: SHA-256 =dfd18965a08d837676591561d730fbe0a9232e940947b07345bd09959d198131=.
- Context :: Authoring context.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: FAIL.
- [audit-design] [BLOCKING] Foundation configuration :: Duration keys, parsing semantics and CSS-tool provisioning were insufficiently explicit for delivery.
- [audit-design] [BLOCKING] Infrastructure ownership :: The state-directory permissions requirement crossed Exodan's ownership boundary.
- [audit-test-definitions] [BLOCKING] Storage oracle :: Synthetic persistent records lacked a defined test migration seam; shutdown exit outcomes were unspecified.
- [audit-spec] [ADVISORY] Packaging :: Give the help source, module path and landing-page scope explicitly.
- Remediation :: Specify configuration and tooling, preserve Exodan directory ownership, define migration fixtures and shutdown outcomes, and capture package/page boundaries. Re-review the amended definition before external audit.

** Local review, round 2
:PROPERTIES:
:ROUND: 2
:END:

- Gate :: definition.
- Revision :: SHA-256 =9f533e7dba59bdfb34da4734d822178113871a03b18805c0ceaefe7784701443=.
- Context :: Authoring context.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: PASS.
- Findings :: No blocking or conditional finding remains in the local review. Configuration, loader failures, Exodan ownership, SQLite restart/migration, HTTP/time bounds, CLI outcomes, rendered accessibility and mixed-tooling verification have defined boundaries and traced checks.
- Remediation receipt :: Round 1 findings are addressed in the configuration table/example, migration fixture definition, infrastructure-owned state-directory policy, explicit exit outcomes, and pinned delivery tooling contract. Tool versions are routine delivery-time selections that must be recorded before use.
- Limitation :: This is local definition evidence only. Independent audit and operator sign-off remain pending; no implementation tests have run.

** External review, round 1
:PROPERTIES:
:CONTEXT: /root/foundation_definition_audit
:ROUND: 1
:END:

- Gate :: definition.
- Revision :: SHA-256 =9f533e7dba59bdfb34da4734d822178113871a03b18805c0ceaefe7784701443=.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: FAIL.
- [audit-spec] [BLOCKING] FND-001 - Local install contract :: Required make install behaviour and verification were missing.
- [audit-spec] [BLOCKING] FND-002 - Observable base URL :: Configuration merging did not prove any foundation link consumed base_url.
- [audit-spec] [BLOCKING] FND-003 - Accessibility oracle :: Name WCAG level, contrast ratios and observable keyboard/semantic requirements.
- [audit-design] [ADVISORY] FND-004 - Defaults compatibility :: Explain why the legacy DEFAULT_CONFIG_PATH fallback is deliberately absent.
- [audit-test-definitions] [ADVISORY] FND-005 - Asset fixtures :: State how valid test assets are provided.

** Local review, round 3
:PROPERTIES:
:ROUND: 3
:END:

- Gate :: definition.
- Revision :: SHA-256 =b8205b4f5f48f21c90edf4453ac0cc94d840d89696b66f531ad7c6589203dae9=.
- Context :: Authoring context.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: PASS.
- Remediation :: Added local PREFIX installation and repeat-install OT, canonical-link HTTP behaviour, WCAG 2.1 AA observations/ratios, explicit runtime-path compatibility rationale, and valid asset fixtures. The independent findings remain open pending retained-context review.
- Findings :: No further local blocking or conditional finding. Implementation and user verification are still future work.

** External review, round 2
:PROPERTIES:
:CONTEXT: /root/foundation_definition_audit
:ROUND: 2
:END:

- Gate :: definition.
- Revision :: SHA-256 =b8205b4f5f48f21c90edf4453ac0cc94d840d89696b66f531ad7c6589203dae9=.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: PROVISIONAL.
- Prior findings :: The retained auditor confirmed FND-001 through FND-005 addressed.
- [audit-test-definitions] [CONDITION] FND-006 - Installation expected evidence :: Add explicit expected managed paths/content, repeat-install preservation, unrelated-file survival, excluded secrets/host config, and installed command behaviour to OT001.1.
- Condition receipt :: Added exactly those outcomes to OT001.1's Expected field. No other substantive change. Verified the revised field against the condition and confirmed sanitize produces identical text.
- Condition-satisfied revision :: SHA-256 =aaf5037d12a574d767eb77d6b4db828705ade01666ff9a5f721406af5bb8b685=, before lifecycle-only DEFINITION_GATE update.
- Effective verdict :: PASS under AUDITS.md's deterministic-condition rule. Operator sign-off and all implementation/user validation remain pending.

** Operator sign-off, 8 September 2026

- Authority :: Taḋg, explicit instruction: "approved all 3. i will build in another session".
- Scope :: Approval of all three specifications, including their labelled proposals.
- Revision :: Audited specification at =ec0bfd8= (three audited build specifications for review); only lifecycle/sign-off metadata changes accompany this receipt.
- Delivery :: Reserved for another session by operator instruction. No code or implementation-test evidence is added.

** Operator-directed removal of Node-based tooling, 8 September 2026

- Authority :: Taḋg: "remove node, there is no justification for it here" and the subsequent instruction to remove its package manager.
- Change :: Withdraw package-managed Stylelint and retired package-audit; specify standalone Biome for CSS checks and retain govulncheck for Go dependencies. Remove NODE from the project profile, the uncommitted retired package-audit commands from Makefile, and the untracked tools/package.json containing only Stylelint dependencies.
- Gate impact :: This material specification amendment resets DEFINITION_GATE and OPERATOR_SIGNOFF to PENDING and moves the foundation work item to REVIEW. Earlier approvals remain historical; counting and workflow definitions are unchanged.
- Scope :: No application implementation changes. The concurrent build session's other edits are preserved. Biome installation and execution have not been claimed.

** Operator clarification of native linters, 8 September 2026

- Authority :: "use the standard sdlc recommended linters (oxlint , tidy-html5, biome)".
- Definition :: Biome checks CSS; tidy-html5 checks rendered HTML without rewriting it; oxlint applies if JavaScript/TypeScript source is later approved. Go checks remain required. Tool provisioning is native and reproducible, with Node-based tooling excluded.
- Evidence :: Specification and one-off validation wording reconciled. This is a documentation amendment, not evidence that the linters have run; the amended definition gate remains pending.

** Architecture refresh, local review round 1, 8 September 2026

- Gate :: definition.
- Artefact :: W001 - Go application foundation.
- Revision :: SHA-256 =697e572072617f5c4921a0a70b3b51e766d46bf1a68a0afcecc4b5ac8edd893b=.
- Context :: Authoring context; all three combined criteria applied to this specification and its shared boundaries.
- Audits :: =audit-spec=, =audit-design=, =audit-test-definitions=.
- Verdict :: PASS (local only).
- Authority :: Operator-approved current foundation and native web-linter selection, followed by the instruction to refresh the three existing specifications. No new work item or application implementation is introduced.
- Scope and receipt :: Native CSS/rendered-HTML checks and bounded failure evidence, current implementation baseline, driver assessment, and previously approved web-tool selection. Existing CLI/configuration/storage criteria and limits are retained.
- Review :: Checked summary/detail agreement, resolved versus historical decisions, requirement/test traceability, Go component boundaries, Exodan-owned configuration and operations, security/privacy and recovery, and proportionate test definitions. No required local correction remains.
- Evidence :: Fourteen documentation files sanitize and parse through Pandoc; 214 local links/anchors resolve with unique heading identifiers. The counting procedure/fixtures/fingerprints and VISION copy are unchanged. Initial CLI/store tests and Makefile were read as implementation evidence, not executed or treated as authority. Concurrent implementation edits are preserved.
- Approval :: Existing product/interface approvals are retained. The operator explicitly approved the web-tool selection before this refresh; that approval does not substitute for the refreshed audit. Any new material decision raised by review requires operator authority.
- External continuation :: One retained external context will review this three-spec refresh, applying each combined gate separately and returning a verdict for each exact specification revision. No prior implementation audit is resumed or replaced by this definition review.

** Architecture refresh, independent runner incident, 8 September 2026

- Gate :: definition; refreshed batch of three existing specifications.
- Context :: One configured external audit invocation through =sdlc-harness start --phase audit=, using installed SDLC v3.1.11 and its configured audit harness/model/timeout. The preceding local-review hash identifies this specification's submitted revision.
- Result :: RUNNER INCIDENT; neither PASS nor FAIL. No fresh independent specification, design or test-definition verdict was accepted.
- Evidence :: The runner exited 1 with =sdlc-harness: audit harness response-malformed: composite verdict requires exactly one non-empty GATE field=.
- Cause of request mismatch :: The submitted instruction requested a separate GATE/verdict block for each of the three specifications. The runner requires one composite GATE field. This formatting request was incorrect; the rejection is not evidence of a specification defect.
- SESSION_ID :: Not returned. The command output supplied no resumable identity. The task's OS temporary directory contains only the submitted refresh diff; no project-local harness context was found through the inspected runtime paths. No replacement reviewer was started.
- Continuation :: Operator direction is required to authorize a replacement review context because the required retained context cannot be addressed. A retry must request exactly one composite verdict and identify per-spec findings within it, or use separately authorized per-spec gates. No prior implementation-audit incident is changed by this definition record.
- Approval boundary :: The approved product and architecture decisions remain recorded. This incident does not grant a current independent gate to materially changed tooling/test definitions; it does not invalidate the unchanged counting rules or their original review evidence.

** Operator approval of refreshed specification, 8 September 2026

- Authority :: Taḋg: "ok approved", after the refreshed specifications were opened for review.
- Scope :: Approval of this refreshed specification, retaining the established product/architecture decisions and native web-tool selection.
- Revision :: Specification at =3f26bde= (refresh verification and audit runner incident checkpoint), followed only by this approval metadata update.
- Gate boundary :: Human approval is recorded separately from the independent audit. The runner incident remains unresolved; no independent PASS or implementation closure is inferred. No replacement audit context has yet been started.

** Operator disposition of minor refresh, 8 September 2026

- Authority :: Taḋg described the changes as a minor lint-tool substitution and stated: "previously these passed, i am happy to mark them approved".
- Accepted revision :: SHA-256 =cd6d0817032aa94f335d1b8075674a46e10f09a0907370f35fb239d723ccf7a2=, before this disposition's metadata updates.
- Disposition :: Retain the prior effective definition PASS and mark the refreshed specification approved on the operator's explicit acceptance of the limited change. No replacement independent audit is required for this refresh.
- Evidence boundary :: The previous independent passing evidence and current local review remain recorded. The failed runner invocation supplied no accepted independent verdict and is not converted into audit PASS. This operator disposition supersedes the earlier pending-gate/replacement-context requests.
- Limits :: Acceptance covers the documented architecture reconciliation and native web-tooling refresh only. It introduces no new voting, privacy or infrastructure policy and does not waive implementation tests, implementation audit, required validation or operator closure.

* Implementation gate

- Execution :: Not started; this batch defines specifications only.

** Operator-directed audit-cycle reset, 8 September 2026

- Authority :: Taḋg explicitly directed: "RESET your audit count" while authorizing hands-off delivery of W001, W002 and W003.
- Disposition :: Preserve all earlier implementation verdicts as historical evidence. The new delivery invocation begins local implementation review at round 1, with at most five local rounds before an external implementation audit is invoked.
- Boundary :: This resets the local-round counter for this authorized delivery invocation only. It does not convert a prior failure into PASS, waive RED/GREEN evidence, validation, or external review, or replace an external context without the applicable audit workflow.

** Local implementation review, reset round 1
:PROPERTIES:
:ROUND: 1
:END:

- Gate :: implementation.
- Revision :: =58848dd= plus the current working tree.
- Context :: Authoring context.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: FAIL.
- [audit-test-code] [BLOCKING] RT001.6 - Limits and shutdown has no retained socket-level coverage for header, read, write and idle limits, graceful draining or expiry outcomes.
- [audit-test-code] [BLOCKING] RT001.2 - Executable HTTP boundary does not cover IPv6 where available, malformed/absent templates, or unsupported static paths.
- [audit-test-code] [BLOCKING] RT001.1 - Layered configuration does not cover empty defaults, empty optional overlays, every duration key, or non-string YAML mapping keys.
- [audit-code] [BLOCKING] AC001.8 - The shutdown path does not distinguish graceful completion from a shutdown deadline and does not report a safe lifecycle outcome.
- Remediation :: Add the missing observable regression coverage, then make the smallest production changes required by the resulting RED failures. Re-run affected tests and this local composite review before external audit.

** Local implementation review, reset round 2
:PROPERTIES:
:ROUND: 2
:END:

- Gate :: implementation.
- Revision :: =e1a57d5=.
- Context :: Authoring context.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: FAIL.
- [audit-test-code] [BLOCKING] RT001.6 - Header, incomplete-body and idle-connection timeouts now use real sockets, and normal signal shutdown is covered. Write-timeout and shutdown-grace-expiry outcomes remain uncovered.
- [audit-test-code] [BLOCKING] RT001.2 - The executable covers the IPv4 listener, actual root/static/health responses and malformed templates. IPv6 remains unexercised on a host that supports it.
- [audit-code] [BLOCKING] AC001.8 - A shutdown deadline still returns a generic context error rather than a defined safe lifecycle diagnostic and exit outcome.
- Remediation :: Define and exercise write-timeout and shutdown-deadline behaviour through the shared server lifecycle, add conditional IPv6 coverage, then rerun this local composite review.

** Local implementation review, reset round 3
:PROPERTIES:
:ROUND: 3
:END:

- Gate :: implementation.
- Revision :: =f661c6e=.
- Context :: Authoring context.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: FAIL.
- [audit-test-code] [BLOCKING] RT001.6 - Socket tests now cover header, incomplete-body and idle limits; graceful shutdown and shutdown-grace expiry have defined, observed outcomes. The write-timeout path remains unexercised at the socket boundary.
- [audit-code] [BLOCKING] AC001.10 - The one-off build/tooling procedure has not yet been recorded against this candidate revision, including Linux cross-compilation and native-tool failure paths.
- Remediation :: Add a bounded socket write-timeout regression test. Then run and record the defined one-off build/tooling checks against the resulting revision before the next local review.

** Local implementation review, reset round 4
:PROPERTIES:
:ROUND: 4
:END:

- Gate :: implementation.
- Revision :: =177e6ea=.
- Context :: Authoring context.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: PASS.
- Receipt :: Regression tests exercise configuration parsing/layering, real SQLite migration and failure recovery, executable CLI and HTTP behaviour, IPv4/conditional IPv6 listeners, static assets, template failure, header/read/write/idle limits and normal/deadline shutdown. One-off tooling validation is recorded for the same candidate series, including isolated native-tool failures and Linux cross-compilation.
- Boundary :: Local evidence is not an effective implementation gate. A retained external composite audit is now required before final validation and operator closure.

** Retained external implementation audit, reset round 4
:PROPERTIES:
:CONTEXT: 01a07f47-11e2-74c1-8161-53c8f9bbbe98
:ROUND: 4
:END:

- Gate :: implementation.
- Revision :: Harness returned SHA-256 =356206229b58195a7140898b778b7f24d9b6cb9c4edbc7f42c2f52522aa04bbc= rather than the submitted current candidate.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: FAIL.
- [audit-test-code] [BLOCKING] RT001.1-RT001.6 coverage, RT001.5 CLI assertions, and RED/GREEN/validation evidence were reported absent.
- [audit-code] [BLOCKING] Rendered-HTML/native-tool evidence and =go.sum= were reported absent.
- Continuation :: The retained context must be resumed. The returned revision and findings conflict with the supplied current evidence bundle, so the resume request will identify the current revision and include =go.sum=.

** Retained external implementation audit, resume runner incident
:PROPERTIES:
:CONTEXT: 01a07f47-11e2-74c1-8161-53c8f9bbbe98
:END:

- Gate :: implementation.
- Result :: RUNNER INCIDENT; no new audit verdict was returned.
- Evidence :: On 2026-09-08T04:32:20Z the retained context attempted to read its evidence through =sed=. Workspace policy rejected that command before the auditor produced a =GATE=, =REVISION= or =VERDICT= block.
- Boundary :: This incident is not implementation evidence and does not replace the retained context. The prior external FAIL remains historical until the same context can assess the supplied current candidate.

** Retained independent implementation audit, runner incident
:PROPERTIES:
:CONTEXT: not issued
:END:

- Gate :: implementation.
- Revision :: Local W001 implementation checkpoint at =74bb4cb= plus the uncommitted rollback-cleanup correction; no audited revision was accepted.
- Context :: Configured retained harness for =gpt-5.6-luna= was invoked once on 2026-09-08.
- Result :: RUNNER INCIDENT. The harness failed before an auditor response because its internal preparation attempted a prohibited =sed= read of =/Users/tigger/.agents/sdlc/MAIN.md=.
- Evidence :: The command returned the workspace policy rejection; no =GATE=, =REVISION=, or =VERDICT= was produced.
- Boundary :: This is neither an audit finding nor PASS/FAIL evidence. Do not create a replacement context. Retry requires operator direction and must use the same retained context if the harness can resume it.

** Retained independent implementation audit, final remediation round
:PROPERTIES:
:CONTEXT: /root/foundation_implementation_audit
:ROUND: 5
:END:

- Gate :: implementation.
- Revision :: Supplied file set SHA-256 =356206229b58195a7140898b778b7f24d9b6cb9c4edbc7f42c2f52522aa04bbc=.
- Audits :: =audit-test-code= and =audit-code=.
- Verdict :: FAIL.
- [audit-test-code] [BLOCKING] RT001.1-RT001.6 remain materially uncovered, including invalid layers, health redaction, unsupported schemas, rollback, limits, shutdown, address families, asset failures and executable-boundary behaviour.
- [audit-test-code] [BLOCKING] CLI tests do not assert required exit codes, stderr diagnostics, no-write behaviour or version content.
- [audit-test-code] [BLOCKING] RED/GREEN and current validation evidence remain absent; =validation.org= is still PENDING.
- [audit-code] [BLOCKING] No current rendered-HTML validation execution or recorded native-tool version/provisioning evidence is supplied.
- Boundary :: This is the fifth failed implementation-gate round. The autonomous remediation loop stops under AUDITS.md; W002 and W003 remain inadmissible for delivery until W001 is remediated and passes its implementation gate.
